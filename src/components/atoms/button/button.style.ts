import {StyleSheet} from 'react-native';

export const styled = StyleSheet.create({
  button: {
    backgroundColor: '#000000',
    borderRadius: 5,
    padding: 10,
    width: '100%',
    alignItems: 'center',
  },
  text: {
    color: '#ffffff',
    fontSize: 16,
    fontWeight: 'bold',
  },
});
