import {StyleSheet} from 'react-native';

export const LoginPageStyled = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'green',
    padding: 20,
    justifyContent: 'center',
  },
});
